---
- name: Install basic applications on systems.
  hosts: all
  become: true

  tasks:
    - name: Update package cache
      apt:
        update_cache: yes

    - name: Install vim, screenfetch, and tmux
      apt:
        name: "{{ item }}"
        state: absent
      loop:
        - vim
        - screenfetch
        - tmux
